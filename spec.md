# 栈桥 · 项目需求说明（spec.md）

> 站点名：**栈桥**（暂定，备案前可改）
> 状态：草案 v1.2 ｜ 最后更新：2026-09-12 ｜ 已完成 4 轮问答，D1 开发完成
> 约定：带 ❓ 的是**尚未确认**的假设；每轮问答后本文档更新一次。

---

## 0. 决策总表

| 项 | 结论 | 轮次 |
| --- | --- | --- |
| 站点名 | **栈桥** | R3 |
| 定位 | UGC 项目社区为主，博客为辅；访客来找项目、看文章 | R1 |
| 用户规模预期 | 慢慢养的小站，不追求爆发流量 | R3 |
| 语言 | **只做中文**，不做国际化 | R3 |
| 注册登录 | 一期邮箱 + 密码；二期手机号；微信登录暂不做（资质限制） | R2 |
| 发布门槛 | **邮箱验证后**才能发布项目；浏览/评论/点赞/收藏只需登录 | R3 |
| 内容审核 | **先审后发**：规则层 + 腾讯云内容安全 + LLM Agent，人工兜底 | R2 |
| 标签体系 | **用户自建标签 + 管理员审核**通过后才公开可用 | R3 |
| 项目表单 | 默认字段集（见 §4.1.1） | R2 |
| 互动 | 点赞、收藏（不分收藏夹）、评论（二级回复）、举报 | R2 |
| 视觉风格 | **GitHub 风格**：信息密度高、克制、以内容为主 | R3 |
| 技术栈 | Nuxt 4 全栈 + PostgreSQL + COS，部署在已有北京 CVM | R1 |
| 预算 | **≤ 100 元/月** | R3 |
| 上线目标 | **一周内**可对外访问的可用版本（备案与域名延后） | R3 |
| 域名与备案 | **暂缓**，先用 `IP:端口` 访问；后续再买域名备案 | R3 |
| 现有资源 | 腾讯云 CVM（北京）、CloudBase 已开通；无域名、无备案、无营业执照 | R1/R2 |
| 服务器环境 | 2 核 2G / 4Mbps 带宽 / CentOS 7.6 / 已装 Nginx（系统待定，见 §7.1） | R4 |
| 邮件通道 | QQ 邮箱 SMTP（2631282516@qq.com），使用授权码发送 | R4 |
| LLM 审核 | DeepSeek API（你已有 Key） | R4 |
| 访问方式 | 备案前用 `http://服务器IP:端口` 访问，已接受 | R4 |
| 交付顺序 | **先本地跑通，再迁移到 CVM**；服务器系统方案与备案一并推迟到上线前决定 | R5 |
| 项目构成 | **大多数项目是 AI 项目**，站点内容与筛选以 AI 为主线 | R6 |
| AI 项目字段 | 模型、模型使用方式、每月成本、每月收益、商业模式、成本与收益说明 | R6 |
| 标签体系 | 分四组：技术栈 / 模型与供应商 / AI 技术 / 应用领域 | R6 |

## 1. 一句话定位

**栈桥**——一个面向中文开发者的项目收录与分享社区。用户注册后可以发布自己的项目、评论互动，站主同步发布技术博客；访客来这里发现项目、阅读文章。

## 2. 目标与范围

### 2.1 P0：一周内必须完成（可对外访问）

| # | 功能 | 验收标准 |
| --- | --- | --- |
| 1 | 项目浏览 | 列表、分类/技术栈筛选、分页、详情页 |
| 2 | 邮箱注册登录 | 注册、登录、退出、邮箱验证、找回密码 |
| 3 | 发布项目 | 填写表单、上传封面/截图、提交进入待审、可在「我的项目」编辑 |
| 4 | 审核流水线 | 规则过滤 + LLM 判定，自动通过/驳回/转人工 |
| 5 | 管理后台 | 审核队列、项目上下架、用户与评论管理 |
| 6 | 互动 | 点赞、收藏、评论（二级回复）、举报 |
| 7 | 个人主页 | 我的项目、我的收藏、我的评论 |
| 8 | 博客 | Markdown 文件导入，列表、标签、详情页（含 SEO meta） |
| 9 | 标签体系 | 用户提交标签申请，管理员审核后可用 |
| 10 | 部署上线 | CVM 上 Docker 运行，通过 IP 访问，可正常注册发帖 |

### 2.2 P1：第二周及以后

- 全文搜索（pg_trgm → Meilisearch）
- 站内通知与邮件通知（投稿结果、评论回复）
- 深色模式打磨、首页精装修、空状态与骨架屏
- 审核数据看板（自动通过率、人工复核量）
- GitHub 仓库信息自动抓取
- 手机号登录、开放多作者博客、关注作者、动态流

### 2.3 非目标

- 不做微信扫码登录（个人主体无资质）
- 不做移动 App、支付、会员、订单
- 不做国际化 / 多语言

## 3. 用户角色与权限

| 角色 | 描述 | 主要能力 |
| --- | --- | --- |
| 游客 | 未登录 | 浏览项目、筛选、阅读博客、看用户主页 |
| 注册用户 | 已验证/未验证邮箱 | 评论、点赞、收藏、举报；**邮箱验证后**可发布项目 |
| 站主 / 管理员 | 你 | 审核项目、审核标签、管理用户与评论、处理举报、发布博客 |
| ❓ 审核员 | 中间角色 | 一期不需要（单人运营），二期视情况 |

## 4. 功能模块

### 4.1 项目（核心）

- 列表页：紧凑卡片流；排序（最新 / 最热 / 编辑推荐）
- 筛选：分类、技术栈标签；搜索框（一期关键词匹配）
- 详情页：标题、作者、封面、正文（Markdown）、截图、外链、仓库地址、点赞 / 收藏 / 评论
- 状态机：`草稿 → 待审 → 自动通过 / 人工通过 → 已发布 → 下架`；`驳回`（附理由，可修改后重投）
- 「我的项目」：列表 + 编辑 + 重新提交 + 查看审核状态与理由

#### 4.1.1 项目提交表单字段（已确认）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 标题 | ✅ | 2～60 字 |
| 一句话简介 | ✅ | 20～120 字，用于卡片与 SEO 摘要 |
| 正文说明 | ✅ | Markdown，介绍功能、亮点、使用方式 |
| 封面图 | ✅ | 1 张，16:9 |
| 截图 | ❌ | 最多 5 张 |
| 分类 | ✅ | 从站主预设分类中选一个 |
| 技术栈标签 | ✅ | 从已通过审核的标签中选，可多选 |
| 仓库地址 | ❌ | GitHub / Gitee 等 |
| 演示链接 | ❌ | 线上可访问地址 |
| 其他外链 | ❌ | 最多 3 条 |

#### 4.1.2 AI 项目专属字段

发布表单顶部提供「这是 AI 项目」开关，打开后展开以下字段。**成本与收益由作者自行填写，页面必须标注「数据由作者提供」**，避免误导访客。

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 使用的模型 | ✅ | 可多选或自定义，例如 `deepseek-chat`、`gpt-4o-mini`、`SDXL 1.0` |
| 模型使用方式 | ✅ | 调用 API / 本地部署 / 混合 |
| 每月成本 | ✅ | 人民币整数，含模型调用、算力、服务器等 |
| 每月收益 | ❌ | 人民币整数；未盈利填 0 |
| 商业模式 | ✅ | 免费 / 免费+付费 / 订阅制 / 一次性买断 / 广告 / 接单服务 / 还没有收入 |
| 成本说明 | ❌ | 例如「按 token 计费，平均 420 元/月」 |
| 收益说明 | ❌ | 例如「订阅制，目前 120 位付费用户」 |

展示规则：

- 列表卡片：AI 徽章 + 模型标签（最多 2 个）+ 月成本 + 月收入 + 盈利状态
- 详情页：独立的「AI 信息」区块，完整展示模型、成本、收益与说明
- 首页：「AI 项目速览」表格，一眼对比模型 / 月成本 / 月收入
- 支持按「AI 项目」筛选，并在首页与列表中优先展示

### 4.2 博客

- 文章列表、标签筛选、时间归档
- 详情页：Markdown 渲染、代码高亮、目录、上一篇 / 下一篇
- 发布方式：仓库内 `content/posts/*.md`，frontmatter 带标题、slug、分类、标签、摘要、发布时间，随代码提交
- SEO：SSR 输出、每篇文章独立 title/description、sitemap.xml、RSS（❓ 是否要 RSS）
- 一期作者仅站主，数据结构预留多作者

### 4.3 用户与认证

- 邮箱 + 密码注册登录；密码 argon2id 哈希
- **邮箱验证**：注册后发送验证邮件；未验证可浏览、评论、点赞、收藏，**不能发布项目**（这是发布门槛，见 §0）
- 找回密码：邮件重置链接，有效期 30 分钟，一次性
- 会话：HttpOnly + SameSite=Lax Cookie 存 session，服务端可失效、可强制下线
- 防护：登录失败限流、注册 IP 限流、异常时触发图形验证码、敏感词过滤
- ❓ 邮件发送通道待定（见 Round 4 问题 2）

### 4.4 内容审核（先审后发 + AI Agent）

```
用户提交
   ↓
① 规则层（同步，毫秒级）
   必填校验 / 链接黑名单 / 敏感词库 / 频率限制（同用户、同 IP、同域名重复提交）
   ↓
② 机器审核（异步，秒级）
   - 腾讯云内容安全：文本与图片违规检测（涉黄涉政、广告违禁）
   - LLM Agent：是否真实项目、内容质量、是否刷站广告、主题相关性
     输出：{ decision: pass | reject | review, score, reasons[], categories[] }
   ↓
③ 决策
   通过 → 自动发布
   违规 → 自动驳回 + 理由，用户可修改重投
   不确定 → 转人工复核队列
```

实现要点：

- **异步执行**：提交立即返回「审核中」，审核在后台跑（一期用 CloudBase 云函数定时任务或应用内 job），用户不等待
- **失败降级**：LLM 超时 / 报错 → **默认转人工**，绝不自动放行
- **可回溯**：保存输入摘要、模型、prompt 版本、原始输出、耗时，便于申诉与调优
- **可调严格度**：拒绝理由模板与判定规则存数据库，随时调整
- **成本**：单次审核在毫元级，小站可忽略

### 4.5 标签体系（用户自建 + 管理员审核）

- 用户发布项目时可**申请新标签**，填写标签名与一句话说明
- 申请进入后台队列，管理员通过后成为公共可用标签；驳回后可看到理由
- 标签表带使用次数，用于热门标签展示与自动排序
- 管理员可合并重复标签、下线滥用标签
- 分类（category）由站主预设并维护，普通用户不能创建（与标签不同）

标签分为四组，发布与筛选时按组呈现：

| 组 | 用途 | 示例 |
| --- | --- | --- |
| 技术栈 | 语言、框架、数据库等实现手段 | TypeScript、Vue 3、PostgreSQL、Docker |
| 模型与供应商 | 用到的模型或提供方 | DeepSeek、OpenAI、Claude、通义千问、SDXL、本地模型 |
| AI 技术 | 用到的技术方向 | RAG、Agent、提示词工程、向量数据库、微调、MCP |
| 应用领域 | 解决的问题域 | AI 编程、AI 写作、知识管理、客服问答、效率工具 |

### 4.6 后台管理

- 审核队列：项目审核、标签审核（通过 / 驳回 + 理由模板）
- 项目管理：编辑、推荐位、下架、删除
- 用户管理：查看、禁言、封禁
- 评论管理：查看、隐藏、删除
- 举报处理：待处理队列、处理结论

### 4.7 互动（一期全做）

- 点赞：项目与文章
- 收藏：仅项目，单一「我的收藏」列表
- 评论：项目与文章，支持二级回复（@被回复人）
- 举报：任意内容可举报，需选择原因分类

## 5. 站点命名、域名与备案

### 5.1 命名

- 站点名：**栈桥**
- 含义：技术栈的「栈」+ 连接项目与人的「桥」
- 备案前可随时改名，代码中的显示名集中在一处配置，避免硬编码

### 5.2 域名与备案（**本期暂缓**）

- 一期不做域名与备案，通过 `http://服务器IP:端口` 访问，供你和朋友测试
- 注意：境内服务器未备案时，部分厂商会对 80/443 端口的 Web 访问做拦截，因此**先用非标准端口**（如 3000 / 8080），并在腾讯云安全组放行
- 后续买域名时的约束：境内备案要求域名后缀在工信部批复名录内，优先 `.com` / `.cn` / `.com.cn` / `.net`，避免 `.io` / `.dev` / `.ai` 等有备案风险的后缀；备案需 1～4 周，与开发并行
- 若将来做 UGC 社区正式对外，需补齐 ICP 备案、公安联网备案、用户协议与隐私政策

## 6. 视觉与交互规范（GitHub 风格）

### 6.1 设计基调

- 信息密度优先，留白克制，让内容和数据成为主角
- 强调「清晰、可信、高效」，不用大面积渐变色和花哨动效
- 边框和分割线用于组织信息，阴影极轻

### 6.2 色板（对齐 GitHub 浅色主题）

| 用途 | 色值 |
| --- | --- |
| 主色 / 链接 | `#0969DA` |
| 主文字 | `#1F2328` |
| 次要文字 | `#656D76` |
| 边框 | `#D0D7DE` |
| 背景 / 次级背景 | `#FFFFFF` / `#F6F8FA` |
| 成功 | `#1A7F37` |
| 危险 | `#CF222E` |
| 警告 | `#9A6700` |

### 6.3 排版与组件

- 字体：`-apple-system, "Segoe UI", "Noto Sans SC", Helvetica, Arial, sans-serif`；代码用等宽字体
- 正文 14px，辅助信息 12px，标题层级 16 / 20 / 24px
- 圆角 6px，边框 1px，卡片 hover 时边框变深而非加阴影
- 按钮：默认（描边）、主色（实心蓝）、危险（红）；输入框 32px 高，聚焦时蓝色描边
- 标签用浅底 chip 样式；列表项紧凑，右侧放数值统计
- 布局：顶部导航 + 内容区最大宽度 1280px；列表页左侧筛选栏 + 右侧内容
- 响应式：移动端单列，筛选栏收进抽屉
- 深色模式：按 GitHub 深色主题对齐，跟随系统并支持手动切换（可放 P1）

## 7. 技术栈

设计原则：单一语言（TypeScript）降低 AI 协作与维护成本；SSR 保证 SEO；关系型数据库；最少组件跑通，按需扩展；全部塞进一台 CVM 以匹配 100 元/月预算。

| 层次 | 选型 | 理由 |
| --- | --- | --- |
| 前端框架 | **Nuxt 4（Vue 3 + Nitro SSR）** | 有 Vue3 基础；SSR/SSG 一体，SEO 友好；AI 生成质量高 |
| UI 与样式 | Tailwind CSS + Nuxt UI | 组件现成，便于实现 GitHub 风格 |
| 后端 API | Nuxt Server Routes（Nitro）同仓全栈 | 一期无需独立后端，少一个服务少一堆运维 |
| 数据库 | **PostgreSQL 16** + Drizzle ORM | 社区数据关系型；迁移与类型安全友好 |
| 认证 | 自建 session + argon2id 密码哈希 | 一套账号体系，便于将来接小程序自定义登录 |
| 内容审核 | 腾讯云内容安全 + LLM API | 国内合规底线 + 质量判断 |
| 异步任务 | 应用内 job + CloudBase 云函数定时触发 | 一期不引入 Redis，省内存与成本 |
| 图片与文件 | 腾讯云 COS（直连，暂不上 CDN） | 省带宽成本；COS 支持图片处理做缩略图 |
| 博客内容 | **@nuxt/content** | 完全匹配 Markdown + Git 提交的工作流 |
| 搜索 | 一期 PostgreSQL 模糊匹配 | 小站够用，二期再上 Meilisearch |
| 后台 | 同应用 `/admin` 路由 + 角色校验 | 无需额外框架 |
| 部署 | CVM 上 Docker Compose（app + postgres），复用宿主机已有 Nginx 反代到 `127.0.0.1:3000` | 单机成本最低，一条命令部署 |
| CI/CD | GitHub Actions 构建镜像 → SSH 部署（❓ 也可先手动部署） | 免费够用 |
| 监控 | 腾讯云云监控 + 可用性探测（❓ 是否接 Sentry 免费版） | 出问题能及时发现 |
| CloudBase 定位 | 定时任务、云存储备选、预览环境 | 用其所长，不与主站抢职责 |

**为什么不用 CloudBase 云数据库 + 云函数为主？** 社区数据关系复杂（用户–项目–评论–标签–收藏），NoSQL + 云函数在事务、聚合、后台管理上吃力；且 SEO 需要 SSR，CloudBase 静态托管做不到。

**关于 Java / C++ 背景**：本项目用不上，强行引入 Spring Boot 会带来双语言维护与更高内存占用，与 100 元/月预算冲突。若将来某块业务需要独立服务，再考虑。

### 7.1 运行环境与部署约束

**CentOS 7.6 已 EOL（2024-06-30）**，有两点硬限制：官方 yum 源已下线（需切 vault 镜像站），且 **Node.js 20/22 官方二进制要求 glibc ≥ 2.28，而 CentOS 7 只有 2.17**，直接在宿主机装 Node 跑不起来。两条路：

- **方案一（推荐）**：控制台重装系统为 **Ubuntu 22.04 LTS**，约 10 分钟；会清空磁盘（当前服务器上除 Nginx 无其他数据），包月实例的公网 IP 通常保留。之后 Docker 与 Node 生态零障碍，也没有 EOL 安全隐患。
- **方案二**：保留 CentOS 7.6，应用**必须跑在 Docker 容器里**（容器自带 Debian 的 glibc，绕开宿主机的限制）。Docker 引擎需装 docker-ce 24.0.x（官方支持 CentOS 7 的最后一个大版本），并切换 vault 源才能装包。能跑通，但宿主机层面长期没有安全更新。
- 两种方案的部署脚本完全一致（Docker Compose），差别只在系统准备阶段，所以先按方案一写脚本、必要时回退到方案二也不会返工。

**2 核 2G 资源分配**：系统 + Nginx ≈ 300MB，PostgreSQL ≈ 400～500MB（`shared_buffers=256MB`、`max_connections=50`、`work_mem=8MB`），Nuxt 应用 ≈ 400～600MB；建议再加 2GB swap 兜底，防止内存抖动导致 OOM。

**4Mbps 带宽是主要瓶颈**（约 500KB/s）：

- 所有图片走 COS 直链 + 图片处理缩略图，绝不从服务器带宽出图
- 前端开启 gzip / HTTP2、控制 JS 体积、静态资源长缓存
- 首页首屏尽量轻量，避免一次加载几十张封面图

**Nginx 复用**：宿主机 Nginx 反代 `127.0.0.1:3000`，应用容器只监听本地回环，对外只暴露必要端口，减少攻击面。

**安全组**：放行对外访问端口。备案前建议使用高位端口（如 8080），规避运营商对未备案域名的 80/443 拦截。

**邮件（QQ 邮箱 SMTP）**：`smtp.qq.com:465`（SSL），需要开启 POP3/SMTP 服务后生成的**授权码**，不是登录密码。个人邮箱有每日发送额度限制（几十到几百封），小站够用。凭证只放环境变量，绝不写进代码仓库。验证邮件用简洁纯文本 + 少量 HTML，降低被判垃圾邮件的概率；正式期若送达率不理想，再换腾讯云邮件推送。

**DeepSeek 审核**：用 `deepseek-chat` + JSON 结构化输出。把固定审核规则放在 prompt 前缀以命中上下文缓存，输入成本可降到约 1/4。单次审核约 1.5k 输入 + 0.3k 输出，约 0.005 元；按每天约 20 次提交估算，**约 3～5 元/月**。必须设置超时与失败降级（转人工），并在上线后评估补充腾讯云内容安全作为合规底线（❓ 免费额度待核实）。

## 8. 数据模型草案

```
users            (id, 昵称, 用户名, 邮箱, 邮箱验证状态, 邮箱验证token,
                  密码哈希, 头像, 简介, 角色[user/admin], 状态[正常/禁言/封禁],
                  注册时间, 最后登录时间, 最后登录IP)
projects         (id, 标题, slug, 简介, 正文, 封面, 截图[], 外链[], 仓库地址,
                  演示链接, 分类id, 状态[草稿/待审/已发布/驳回/下架],
                  审核来源[自动/人工], 审核理由, 是否推荐, 浏览量, 点赞数,
                  收藏数, 评论数, 作者id, 发布时间, 创建/更新时间,
                  是否AI项目, 模型[], 模型使用方式[API/本地/混合],
                  每月成本, 每月收益, 商业模式, 成本说明, 收益说明)
project_tags     (project_id, tag_id)                   多对多
tags             (id, 名称, slug, 分组[技术栈/模型/AI技术/应用领域], 说明,
                  状态[待审/已通过/已驳回], 使用次数, 申请人id, 审核人id,
                  审核理由, 创建时间)
categories       (id, 名称, slug, 描述, 排序)            站主维护
comments         (id, 目标类型[项目/文章], 目标id, 用户id, 内容,
                  父评论id(NULL=一级), 根评论id, 被回复用户id,
                  状态[正常/隐藏/删除], 创建时间)
likes            (user_id, 目标类型, 目标id, 创建时间)    唯一(user_id,目标类型,目标id)
favorites        (user_id, project_id, 创建时间)          唯一(user_id,project_id)
reports          (id, 举报人id, 目标类型, 目标id, 原因分类, 补充说明,
                  状态[待处理/已处理/已驳回], 处理人, 处理时间)
moderation_logs  (id, 目标类型, 目标id, 阶段[规则/内容安全/LLM/人工], 输入摘要,
                  模型与prompt版本, 原始输出, 决策, 耗时, 创建时间)
sessions         (id, user_id, token, 过期时间, IP, UA)
posts            (博客来自 Git；frontmatter: 标题, slug, 摘要, 分类, 标签, 作者, 发布时间)
```

## 9. 成本预算（目标 ≤ 100 元/月）

| 项目 | 费用 | 说明 |
| --- | --- | --- |
| CVM 服务器 | 已有（约 60～90 元/月，视你现有配置） | 应用 + 数据库共用一个实例 |
| 域名 | 约 6 元/月（70 元/年） | 暂缓购买 |
| ICP 备案 | 免费 | 暂缓办理 |
| COS 对象存储 | 1～5 元/月 | 图片存储 + 下行流量，小站量级 |
| LLM 审核 | 3～5 元/月 | 按每天约 20 次提交估算，单次约 0.005 元 |
| 邮件发送 | 0 元 | 免费额度或第三方 SMTP |
| 短信 | 0 元 | 一期不启用 |
| **合计** | **约 70～100 元/月** | 在预算内，且未算 CDN（暂不上） |

## 10. 里程碑与一周排期

### 10.1 一周冲刺（P0）

| 天 | 任务 | 产出 |
| --- | --- | --- |
| D1 | 环境准备 + 项目骨架 + 部署链路 | 系统就绪、swap、Docker；Nuxt + PG 跑在 CVM，IP 可访问「Hello 栈桥」 |
| D2 | 数据模型 + 认证 | 注册、登录、邮箱验证、找回密码全通 |
| D3 | 项目浏览 | 列表、筛选、分页、详情页（含种子数据） |
| D4 | 项目发布 | 表单、图片上传到 COS、我的项目、编辑与重投 |
| D5 | 审核流水线 + 后台 | 规则 + LLM 审核，后台审核队列与上下架 |
| D6 | 互动 + 个人主页 | 点赞、收藏、评论二级回复、举报、我的收藏 |
| D7 | 博客 + 首页 + 打磨 | Markdown 文章上线、首页、移动端适配、上线验收 |

### 10.2 上线阶段前置条件（本地跑通后再准备）

| 项 | 状态 |
| --- | --- |
| 服务器配置（2 核 2G / 4Mbps / CentOS 7.6） | ✅ 已确认 |
| 系统方案（重装 Ubuntu 22.04 或保留 CentOS 7 + Docker） | ❓ 待你决定（见 Round 5） |
| CVM 的 SSH 登录方式（IP、用户、密钥或密码） | ❓ 待提供 |
| QQ 邮箱 SMTP 授权码 | ❓ 待你生成并提供 |
| DeepSeek API Key | ❓ 已有，待提供 |
| COS 存储桶（桶名、地域、密钥） | ❓ 待创建/提供 |
| 腾讯云安全组放行端口 | ❓ 待你操作 |

### 10.3 备案与域名（P1，本周不做）

- 买域名 → 提交 ICP 备案 → 配置 DNS 与 HTTPS → 正式对外

### 10.4 进度记录

**D1 已完成（本地开发环境）**

- 技术骨架：Nuxt 4 + TypeScript + Tailwind CSS 4（GitHub 风格色板与排版规范已落地）
- 数据层：Drizzle ORM + 完整数据模型（11 张表），迁移脚本与种子数据脚本
- 本地数据库：PGlite（WASM 版 PostgreSQL 16），无需安装 Docker 或 PostgreSQL；线上切换只改 `NUXT_DATABASE_URL`
- 页面：首页（热门/最新/分类/标签）、项目列表（分类、技术栈、排序、搜索、分页）、项目详情（Markdown 正文、作者、评论与二级回复）、关于页、博客占位页、404 页
- 接口：`/api/projects`、`/api/projects/[slug]`、`/api/categories`、`/api/tags`、`/api/cover/[slug]`（动态生成占位封面）
- 示例数据：6 个分类、16 个标签、4 个用户、8 个项目、5 条评论（含二级回复）
- 已验证：筛选、搜索、排序、分页、浏览量自增、Markdown 渲染与 XSS 白名单过滤、开发模式与生产构建均正常

**D1.5 已完成（AI 需求变更）**

- 项目模型新增 AI 字段：是否 AI 项目、模型列表、模型使用方式、每月成本、每月收益、商业模式、成本与收益说明
- 标签体系改为四组：技术栈 / 模型与供应商 / AI 技术 / 应用领域（39 个标签）
- 列表卡片显示 AI 徽章、模型、月成本、月收入与盈利状态
- 首页新增「AI 项目速览」表格，可横向对比模型与收支
- 详情页新增「AI 信息」区块，含成本/收益说明与「数据由作者提供」免责声明
- 列表页新增「全部 / AI 项目」筛选与分组标签侧边栏
- 示例数据改为 10 个项目（7 个 AI 项目 + 3 个非 AI），9 条评论
- 已实测 17 项检查全部通过

**D1.6 已完成（需求补充）**

- 成本与收益字段对**所有项目**开放且全部选填，不做成本分项
- 新增「总收入」字段，与「每月收入」并列展示
- 新增排行榜页 `/ranking`：利润榜（月收入 − 月成本）、收入榜、成本榜
- 首页改为「项目收支速览」，含月成本 / 月收入 / 总收入 / 月净利
- 开发体验：开发服务器启动自动应用迁移；新增 `POST /api/dev/seed` 可在不重启服务器的情况下重灌数据

**D2 已完成（注册登录）**

- 邮箱 + 密码注册、登录、退出，会话存数据库并以 HttpOnly Cookie 承载（30 天有效）
- 邮箱验证：注册即发验证邮件，24 小时有效，验证后可发布项目（`requireVerifiedUser`）
- 找回密码：邮件重置链接 30 分钟有效，重置后强制所有设备重新登录
- 密码哈希改用 Node 内置 scrypt（N=32768, r=8, p=1），理由见 §7 技术栈说明
- 登录、注册、发信均有基于 IP / 用户的内存限流
- 账号创建脚本 `npm run user:create`；本地开发另提供 `/api/dev/outbox`、`/api/dev/grant-admin`
- 页面：注册、登录、找回密码、重置密码、邮箱验证、个人中心；页头按登录态切换用户菜单
- 已实测 12 项接口流程 + 6 项页面渲染检查全部通过

**D3 已完成（发布项目）**

- 发布入口：页头、首页与 `/projects/new`；邮箱未验证时引导去验证
- 表单字段：基础信息、Markdown 正文（服务端同源预览）、封面与截图上传、分类与四组标签、
  新标签申请（进审核队列）、相关链接、AI 开关（模型 + 使用方式）、成本与收益全字段
- 图片上传：`POST /api/upload`，本地存入 `public/uploads/年月/`，限 JPG/PNG/WebP/GIF 且单张 ≤ 5MB
- 投稿流：`存为草稿` / `提交审核`；普通用户进待审队列，管理员发布直接上线；
  已发布项目被作者修改后回到待审核状态（D5 接入 AI 审核后由流水线接管）
- 个人中心「我的项目」：状态标签、审核意见、编辑与查看；`/projects/edit/[slug]`
- 标签使用次数按已发布项目实时重算
- 已实测 11 项接口 + 页面渲染检查通过

**下一步（D5）**：内容审核流水线（规则层 + 腾讯云内容安全 + DeepSeek）与审核后台。

## 11. 待确认问题清单

### Round 1 —— 已完成

UGC 项目社区定位；一期仅站主写博客、Markdown + Git 发布；AI 为主开发；CVM 北京 + CloudBase 已开通

### Round 2 —— 已完成

邮箱密码登录（二期手机号，微信不做）；AI 先审后发；项目表单按默认集；互动一期全做含二级回复；命名候选

### Round 3 —— 已完成

站点名「栈桥」；GitHub 风格；小站慢慢养；预算 ≤ 100 元/月；标签用户自建 + 管理员审核；邮箱验证才能发项目；一周内上线；只做中文；域名备案延后

### Round 4 —— 已完成

1. 服务器：2 核 2G、4Mbps、CentOS 7.6、已有 Nginx
2. 邮件：QQ 邮箱 SMTP
3. LLM：已有 DeepSeek API Key
4. 访问方式：接受备案前用 IP + 端口
5. 一周排期与 P0/P1 取舍：认可

### Round 5 —— 部分回答

1. 交付顺序：先本地跑通，确认没问题后再上线服务器 ✅
2. 服务器系统方案（重装 Ubuntu 22.04 还是保留 CentOS 7.6）⏸ 推迟到上线前决定，不影响当前开发

### Round 6 —— 已完成

1. 项目构成以 AI 为主，标签按 AI 方向重新组织 ✅
2. AI 项目需展示模型、成本、收益 ✅

### Round 7 —— 待回答

1. 成本颗粒度：是否要区分「一次性投入 / 每月成本 / 算力、API、服务器分项」
2. 收益口径：是否需要区分「月收入 / 总收入 / 是否回本」
3. 是否给 AI 项目做排行榜（如「最省钱」「最赚钱」）
4. 非 AI 项目是否也要填成本与收益（当前设计只对 AI 项目展示）
